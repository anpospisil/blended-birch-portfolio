import { Box} from "@chakra-ui/react";
import {Picture} from "../../components/Carousel";
import "../../App.css"

export const Portfolio = () => {
  return (
    <Box mt={20}>
      <Picture />
      </Box>
  );
  };
